{
  "name": "pokemmo-link-monorepo",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:web\" \"npm run dev:agent\"",
    "dev:web": "npm run dev --prefix apps/web",
    "dev:agent": "cd apps/agent && gradlew.bat runSnooper"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}